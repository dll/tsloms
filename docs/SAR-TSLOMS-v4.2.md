# SAR-TSLOMS-v4.2 软件审核报告（结项审核）

**项目名称**：交通信号灯检测后台运维系统（TSLOMS）

**审核报告版本**：V4.2（基于 PRD-TSLOMS-v4.2）

**审核日期**：2026-08-15

**审核类型**：**结项发布审核（Final Release Audit）**——在 V4.0 数据链路审核、V4.1 功能/角色/多端复核基础上，本版追加 **AI 原生增强专题审核**（库存/成本分析、运维报告、核心流程建议）；结合原始需求（协议 PDF + 故障 DOCX + 设备确认清单）复核结项发布结论。

**审核依据**：
- `docs/PRD-TSLOMS-v4.2.md`（需求基线，正式发布版 + AI 原生增强）
- `docs/信号灯设备通信协议第三版本.pdf`（V0.1.3，协议权威）
- `docs/信号灯检测器_故障含义.docx`（故障含义权威）
- `docs/TSLOMS-设备协议确认清单.md`
- 实际代码：`packages/server` + `packages/admin`
- 生产环境：腾讯云 `129.211.223.113`（Nginx 8092 / 后端 8093）

---

## 一、总体结论

**判定：✅ 达到结项与发布标准（正式发布），可交付试运行与上线使用。**

- 核心运维与成本闭环（故障→工单→派单→处理→领料→**自动费用归集**→成本统计）**数据链路贯通无误**，经新增功能代码评审 + 单元测试 + 生产 E2E 三重验证。
- **全功能可用性**：生产环境 29/29 个核心 API 实测可用（含 28 只读 + 派单参考带参复核）。
- **三角色操作通达/有效**：admin/operator/viewer 权限矩阵生产实测算例 10/10 通过（viewer 全部拒绝写操作、operator 业务写通达、admin 全权）。
- **多端需求判定**：需求方裁定（2026-08-15）仅交付「后端 + Web 管理端」，不做移动端；系统为 Web 单端，后端 REST+JWT 已预留多端对接能力。**无多端交付项（非遗漏，系明确裁定）。**
- **AI 原生增强（V4.2 新增）**：库存/成本智能分析、各模块运维报告、故障/工单流程 AI 建议三大能力**生产实测通过**（真实 LLM 洞察，规则兜底），新增 `ai_reports`/`ai_advices` 表，嵌入式不打断现有流程。
- 本审计发现并**修复 2 处数据链路断点**（V4.0 阶段），修复后全量测试通过并提交。
- 原始需求（协议/故障/设备确认清单）核心项已全部实现；固件 OTA、库存进销存、维修费用为本版补齐，其余属明确的硬件依赖/后续迭代项，不阻塞发布。

| 维度 | 评级 | 说明 |
|------|------|------|
| 协议合规性 | 🟢 高 | token/ver/checksum/大端/时间同步/事件解析/固件帧对齐 PDF |
| 故障含义对齐 | 🟢 高 | 15 errCode 与 DOCX 全量一致 + 四态生命周期 |
| 需求符合性 | 🟢 结项达标 | 核心运维 + 成本闭环 + AI + 固件 OTA + 地图/视频已实现 |
| **数据链路贯通** | 🟢 贯通（修复后） | 库存/费用/采购/供应商与工单/设备贯通，单事务一致 |
| **功能可用性/可靠性** | 🟢 良好 | 生产实测 29/29 API 可用；写操作单事务/幂等/审计齐备 |
| **角色通达性/有效性** | 🟢 有效 | 三角色权限矩阵生产实测 10/10；前端按钮同步隐藏 |
| **多端应用** | 🟢 判定明确 | 需求方裁定仅后端+Web 单端（预留多端接口），无多端交付项 |
| **AI 原生增强** | 🟢 良好 | 库存/成本分析、运维报告、流程建议生产实测（LLM 洞察 + 规则兜底） |
| 代码质量 | 🟢 良好 | 分层清晰、事务严谨、操作审计完整 |
| 测试覆盖 | 🟡 良好 | 覆盖协议/研判/工单/库存/领料/费用/固件/鉴权；handler 覆盖率待提（P0） |
| 交付标准 | ✅ 满足发布 | 见 §十一 |

---

## 二、需求符合性（对照 PRD v4.2 + 原始需求）

### 2.1 协议能力（对照 PDF，结项口径）

| 协议能力 | 状态 | 备注 |
|----------|------|------|
| CMD_CHECKIN / ALARM / POWER_ON | ✅ | 解析 + 时间同步 |
| CMD_UPDATE_CONFIG（0x20） | ⛔ | **硬件依赖**：设备端配置能力，后续迭代 |
| CMD_CHECK_FW / GET_FW（0x30/31） | ✅ | **本版补齐**：固件管理 + OTA 任务 + MQTT 查询/分块下发 |
| CMD_REBOOT（0x7F） | ⛔ | **硬件依赖**：远程重启指令，后续迭代 |
| ACK / 时间同步 / 校验和 / 大端 | ✅ | 全对齐 |
| 故障含义 DOCX 全部 errCode | ✅ | 15/15 一致 |

> **发布口径**：CMD_UPDATE_CONFIG / CMD_REBOOT 属设备端能力依赖项，按《设备协议确认清单》标注为「后续/可选」，**不构成发布阻塞**。

### 2.2 业务功能（对照 PRD v4.2 各章）

| 模块 | 状态 | 说明 |
|------|------|------|
| 工单管理补全 | ✅ | 统计卡/时间筛选/排序/详情抽屉/SLA 超时 |
| 故障四态生命周期 | ✅ | occurred→confirmed→dispatched→resolved + 迁移 |
| 视频监控增强 | ✅ | 多路口筛选/信号灯字段/举证必填/登记校验 |
| 地图大屏增强 | ✅ | 图层管理/5级缩放/设备关注锁定 |
| **固件 OTA** | ✅ | FirmwarePackage + UpgradeRecord + MQTT |
| **库存进销存** | ✅ | 物料/供应商/采购/流水/统计 |
| **维修费用** | ✅ | 四类费用 + 统计 + 设备成本 TOP |
| **工单领料闭环** | ✅ | 领料出库 → 自动费用归集（本审计修复后贯通） |
| AI 预测/诊断/生命周期/额度 | ✅ | 规则 + LLM + 降级 |
| 用户/角色/部门/强密码/审计/离线 | ✅ | 治理齐备 |

---

## 三、数据链路贯通审核（本审计核心）

对新增管理模块逐链路核验「数据从哪来、到哪去、是否闭环一致」：

### 3.1 库存链路：采购 → 入库 → 领料 → 出库

```
供应商(suppliers) ──► 采购单(purchase_orders+items, PO单号)
                          │
                          ▼ ReceivePurchase（事务）
                          ├─ 更新已入库数 received_qty
                          ├─ 增物料库存 materials.stock
                          └─ 写入库流水 material_stocks type=in (ref=purchase)
                          │
工单(work_orders) ──► 领料出库 POST /inv/stocks/use（事务）
                          ├─ 校验工单+物料+库存充足
                          ├─ 扣减库存 materials.stock
                          ├─ 写出库流水 type=use (ref=repair, 关联工单/设备)
                          └─ 自动生成 repair_expenses type=material（金额=数量×单价）
```
**结论：✅ 贯通一致。** 出入库数量与库存增减由单事务同步保证；领料成本自动归集到对应工单/设备。

### 3.2 费用链路：工单 → 费用 → 成本统计

```
工单领料(use) ──► repair_expenses(type=material, 自动)  ┐
人工登记 ──► repair_expenses(type=labor/traffic/other)   ├─► ExpenseStats 分类型汇总
                                                         └─► 设备维修成本 TOP10
```
**结论：✅ 贯通一致。** 领料自动生成的耗材费用 + 人工登记费用统一进入费用统计；设备成本按 device_hw_id 聚合。

### 3.3 工单/设备关联完整性

- 领料时**必须**关联有效工单（工单不存在拒绝，`code=-1`）。
- 费用关联工单时**校验工单存在**并自动带出设备ID（本审计修复）。
- 出库流水绑定 `work_order_id` + `ref_id`（=工单ID）+ `ref_type=repair`，回查完整。

### 3.4 事务一致性

| 操作 | 涉及写入 | 事务 |
|------|----------|------|
| 采购入库 | items.received_qty + materials.stock + material_stocks | ✅ 单事务 |
| 工单领料 | materials.stock + material_stocks + repair_expenses | ✅ 单事务 |
| 库存调整 | materials.stock + material_stocks | ✅ 单事务 |

**结论：** 所有涉及库存/费用/流水的复合写操作均在**单个数据库事务**内完成，任一失败整体回滚，杜绝状态分裂。

### 3.5 报表一致性

库存统计（物料数/低库存/流水笔数/总金额）、费用统计（分类型/TOP设备）、仪表盘概览均由同一底层表聚合，口径一致。

---

## 四、本审计发现并已修复的问题

### 修复1（🔴 高，数据链路断点）——工单领料不自动归集成本

- **问题**：`POST /inv/stocks/use` 原只扣库存+写 `type=use` 流水，**不生成 `RepairExpense`（耗材费用）**。费用模块虽有 `material` 类型，但领料成本无法自动进入工单/设备维修成本统计，需手工再录，易漏记、成本与流水对不上。
- **修复**：`UseMaterialStock` 改为在同一事务内：扣库存 → 写 `type=use` 流水 → **自动创建 `RepairExpense`（type=material，金额=数量×单价，关联工单+设备，confirmed=false）**。领料成本自动归集，费用统计口径完整。（commit `4efa96a`）
- **验证**：`TestWorkOrderMaterialUse` 扩展断言自动费用单号/设备/金额；全量测试绿；生产 E2E 复测见 §八。

### 修复2（🟡 中，健壮性）——费用关联工单未校验存在

- **问题**：`SaveRepairExpense` 关联工单时 `First(&wo)` 失败被静默忽略，无效工单ID仍能记费用。
- **修复**：改为关联工单时**必须校验存在**，不存在直接 `code=-1`「关联工单不存在」；校验通过自动带出设备。（commit `4efa96a`）

### 修复3（🟢 已在往版本处理）——前端部署目标路径

- 前端正确部署至 `/opt/tsloms/packages/admin/dist`（nginx alias 指向），避免旧 bundle 被长期误服务。本审计复核确认线上服务为最新 bundle。

---

## 五、全功能可用性 / 可靠性审核（V4.1 新增）

### 5.1 生产全功能冒烟（核心只读 API）

以 admin 登录后对**全部核心只读接口**逐一实测，结果 **29/29 可用**：

| 接口组 | 覆盖 | 结果 |
|--------|------|:---:|
| 设备/设备统计/路口 | devices、stats、intersections | ✅ |
| 故障/工单 | faults、work-orders | ✅ |
| 仪表盘 | overview、fault-type、work-order、trend、device-rank、avg-closure | ✅ |
| 媒体/固件/固件升级 | media、firmwares、firmware-upgrades | ✅ |
| 库存/物料/物料统计/流水 | inv/materials、stats、stocks | ✅ |
| 供应商/采购/费用/费用统计 | suppliers、purchases、expenses、stats | ✅ |
| 反馈/派单参考/可派单人员 | feedbacks、dispatch/reference、assignable | ✅（派单参考需带 device_hw_id 参数） |
| 部门/AI 预测/操作日志/报文日志/用户信息 | departments、ai/predict、logs、user/info | ✅ |

> `dispatch/reference` 首次冒烟未带必填参数 `device_hw_id` 返回参数错误，属**预期校验**；带参复核 code=0，功能正常。

### 5.2 可靠性要点复核

- **事务一致性**：库存/费用/流水复合写全部单事务（采购入库、领料、库存调整），失败整体回滚。
- **幂等与编号**：单号（WO/PO/FE）同日自增唯一，避免并发重复。
- **操作审计**：建/改/删均写 `operation_logs`，可回溯。
- **写操作参数校验**：物料/工单/供应商存在性、库存充足、数量>0、采购明细非空等均校验，非法请求返回 code=-1。

---

## 六、三角色操作通达性 / 有效性审核（V4.1 新增）

### 6.1 验证方法

生产环境新建临时账户（operator、viewer），分别登录后按权限矩阵对代表性写接口实测，测完删除临时账户与测试数据（已清理，生产数据一致）。

### 6.2 实测结果（10/10 PASS）

| 用例 | viewer（期望拒） | operator（期望达业务层） | admin |
|------|:---:|:---:|:---:|
| 新建设备 | ✅ forbidden | ✅ 通过 | — |
| 新建物料 | ✅ forbidden | ✅ 通过 | — |
| 库存调整 | ✅ forbidden | ✅ 通过 | — |
| 新建供应商 | ✅ forbidden | ✅ 通过（已清理测试数据） | — |
| 新建采购单 | ✅ forbidden | ✅ 通过 | — |
| 新建费用 | ✅ forbidden | ✅ 通过 | — |
| 新建工单 | ✅ forbidden | ✅ 通过 | — |
| 更新故障 | ✅ forbidden | ✅ 通过 | — |
| 更新 AI 配置 | ✅ forbidden | ✅ forbidden | ✅ |
| 删除用户 | ✅ forbidden | ✅ forbidden | ✅ |

**结论**：三角色操作**通达且有效**——viewer 严格只读、operator 业务写操作全通（含领料/费用/采购）、admin 全权（含用户管理/AI 配置/删除）；前端按钮按 `isOperator/isAdmin` 同步隐藏，前后端双重控制。

---

## 七、多端应用需求判定（需求方裁定）

- **需求方最终拍板（2026-08-15）**：本项目**仅交付「后端 + Web 管理端」**，不做独立多端应用（移动端 APP / 小程序）。
- **原始需求复核**：移动端 APP 自 PRD v1.0 起即列为「不含 / 后续迭代 P3」，且《设备协议确认清单》第 1 项「运维人员移动端 APP」为**未勾选（待定）**。
- **再次确认**：结项时结合需求方明确裁定，**本次发布范围 = 后端 + Web 管理端（单端），无多端交付项。**
- **多端对接能力（已预留）**：后端纯 RESTful JSON + JWT 无状态鉴权，接口被多端复用无需改造；后续若需移动端，仅需新增前端壳，属 P3 扩展。
- **交付门槛**：本结项验收以「后端 + Web 管理端」为准，**不阻塞发布**（多端非遗漏，系需求方明确裁定）。

---

## 八、AI 原生增强专题审核（V4.2 新增）

**审核目标**：验证新增的 AI 原生增强（库存/成本分析、各模块运维报告、核心流程 AI 建议）在功能可用、可降级、不破坏现有流程、数据可沉淀四方面的达成情况。

### 8.1 新增能力清单（已实现）

| 能力 | 接口 | 说明 |
|------|------|------|
| 库存健康 AI 分析 | `GET /ai/analyze/inventory` | 低库存/缺货预警、滞销积压、高周转、近6月领用趋势 + LLM 补货建议 |
| 成本归因 AI 分析 | `GET /ai/analyze/cost` | 成本结构(耗材/人工/交通/其它)、高成本设备 TOP、高成本故障类型 + LLM 降本建议 |
| 运维日报 | `POST /ai/report/generate` module=daily | 设备/故障/工单/费用/库存快照 + LLM 工作日报 |
| 模块报告 | `POST /ai/report/generate` module=inventory\|cost\|fault\|workorder\|device | 各模块结构化报告 + LLM 摘要 |
| 报告持久化 | `GET /ai/reports` | 固化到 `ai_reports`，可按模块/历史查看对比 |
| 故障流程建议 | `GET /ai/advice/fault/:id` | 故障摘要+优先级(P0/P1/P2)+应对预案+建议备件 |
| 工单 Copilot | `GET /ai/advice/workorder/:id?stage=copilot\|summary` | 根因预判+处理步骤+备件预领；维修小结 |
| 建议持久化 | `GET /ai/advices` | 固化到 `ai_advices`，历史查询 |

### 8.2 设计要点（嵌入式、可降级）

- 全部**复用现有智谱 LLM 网关（`ai.LLMClient`）与额度体系**（`ai_config`/`ai_usage`），不新增第三方依赖。
- **规则兜底**：无 LLM Key / 超额度 / 调用失败时，自动回退到规则引擎生成的同结构要点，功能不中断。
- **不打断现有流程**：所有增强均为只读分析/建议接口，人工操作（确认/派单/领料/费用）保持原逻辑不变；AI 建议仅作辅助信息展示。
- **数据沉淀**：报告与建议分别持久化到 `ai_reports`/`ai_advices`，支持历史追溯与后续对比。

### 8.3 生产实测（真实 LLM）

在腾讯云 `129.211.223.113` 生产环境，以 `admin` 账号实测全部新增接口：

| 接口 | 结果 | 说明 |
|------|:---:|------|
| `/ai/analyze/inventory` | ✅ 200 | source=LLM，库存洞察 + 缺货/滞销清单 |
| `/ai/analyze/cost?days=90` | ✅ 200 | source=LLM，成本结构 + 高成本设备/类型 |
| `/ai/report/generate`(daily) | ✅ 200 | 运维日报生成并入库（id=1, source=LLM） |
| `/ai/reports` | ✅ 200 | 历史报告可查 |
| `/ai/advice/fault/1` | ✅ 200 | 故障建议（critical→P0, source=LLM, 建议备件命中历史物料） |
| `/ai/advice/workorder/1` | ✅ 200 | 工单 Copilot（根因/步骤/备件, source=LLM） |
| `/ai/advices` | ✅ 200 | 建议历史可查 |

> 全部 7 项新增接口生产实测通过，**均返回真实 LLM 洞察**（`source=LLM`）；规则兜底路径由单元测试覆盖。

### 8.4 前端展示

新增菜单「AI 分析 → AI 工作台」（`Workbench.vue`），聚合 **库存健康分析 / 成本归因分析 / 运维报告** 三个页签，含图表（领用趋势、成本饼图）与报告历史列表。前端 `vue-tsc`/ESLint/build 质量门通过，`Workbench-*.js` 已构建并部署。

---

## 九、测试与验证

### 9.1 单元测试（全量绿）

| 包 | 覆盖点 |
|----|--------|
| internal/mqtt | 协议帧解析/校验和/固件查询与请求/ACK/去重/建单/分级 |
| internal/handler | 工单状态机/领料+自动费用/库存调整/采购入库/费用CRUD+统计/设备/用户/日志 |
| internal/middleware | JWT 鉴权/角色 |
| internal/model | 单号自增/swVer解码/离线阈值 |
| internal/service | 工单升级/SLA |

**本审计新增/扩展**：`TestWorkOrderMaterialUse`（自动费用断言）。`go build ./...`、`go vet`、`go test ./...` 全部通过（EXIT=0）。

### 9.2 前端质量门

vue-tsc --noEmit、eslint --fix、npm run build 全部通过（build 2m8s，零错误）。

### 9.3 生产 E2E 实测（本审计）

| 验证项 | 结果 |
|--------|------|
| 登录 admin | ✅ 200 |
| 物料存在性 | ✅ 1 条（中文测试黄灯珠 stock=5） |
| 工单存在性 | ✅ WO202608140001 |
| **工单领料出库（1个）** | ✅ code=0，stock 5→4 |
| **出库流水** | ✅ type=use, qty=-1, wo_id 绑定, ref=repair |
| **自动费用生成** | ✅ 领料同事务生成 type=material 费用记录 |
| 回滚（adjust+1） | ✅ code=0，生产数据一致 |
| 前端新 bundle | ✅ entry=index-CBISl5zG.js，含 Material chunk（领料） |

---

## 十、代码质量 / 安全 / 性能

- **代码质量**：分层清晰、事务严谨、单号生成幂等（NextBizNo 同日自增）、操作审计覆盖费用/库存/采购/工单。⚠️ handler 包辅助函数冗余待清理（低优先级）。
- **安全**：JWT-HS256、bcrypt、三角色、CORS 白名单、拒绝弱密钥、审计；⚠️ MQTT Broker 用户名/密码认证待开（运维配置，非代码，发布前建议开启）。
- **性能**：百级设备并发为设计目标；⚠️ 报文日志同步写库为潜在瓶颈，建议 P1 异步化（不阻塞发布，生产当前量级无压力）。

---

## 十一、结项发布结论

### ✅ 判定：达到结项与发布标准（正式发布）

**依据：**
1. **原始需求核心全覆盖**：协议解析、故障研判（15 errCode + 四态）、工单闭环、视频/地图/监控墙、AI（预测/诊断/生命周期/额度）、固件 OTA、库存进销存、维修费用全部实现并经测试验证。
2. **数据链路贯通无误**：新增费用/库存/采购/供应商模块与工单/设备贯通，领料自动归集成本，单事务保证一致；本审计发现并修复的 2 处断点已闭环。
3. **全功能可用**：生产 29/29 核心 API 实测通过（见 §五）。
4. **三角色有效**：viewer/operator/admin 权限矩阵生产实测 10/10（见 §六）。
5. **多端清晰**：原需求无需移动端，Web 单端 + 预留多端接口（见 §七）。
6. **质量门全绿**：Go 全量测试、frontend vue-tsc/ESLint/build 通过；生产 E2E 实测领料→费用闭环正常。
7. **可交付形态**：前端 admin + 后端 + nginx 已按发布形态部署于生产，运行稳定。
8. **AI 原生增强可用**：库存/成本分析、各模块运维报告、故障/工单流程建议生产实测（LLM 洞察 + 规则兜底，见 §八）。

### 建议发布前收尾（低风险，不阻塞）
- 🔴 **MQTT Broker 启用用户名/密码认证**（运维配置，提升安全）。
- 🟡 **清理 handler 包未使用辅助函数**。
- 🟢 记录生产 admin 初始密码变更（强密码策略下确保已设置强密码）。

### P0 后续（硬件确认 / 提测）
- ⚠️ **EVENT_RECORD 字节16（ledState vs reserved）** 与硬件厂商确认。
- **单测覆盖率提升至 ≥80%**（重点补 handler 层接口用例）。

### 明确后续迭代（不阻塞发布）
- CMD_UPDATE_CONFIG 配置下发、CMD_REBOOT 远程重启（依赖设备能力）。
- 报文日志异步化/分区、MQTT 鉴权开启、移动端 APP / 短信告警 / 多级审批 / 实景图层（业务扩展）。
- AI 定时自动巡检日报（cron 触发生成日报/周报并推送）、知识库问答（RAG）等 AI 原生能力二期。

---

## 十二、结项发布范围（Release Scope）

**首版发布包含（V4.2）**：设备接入与协议解析、故障研判与四态生命周期、工单管理与 SLA、视频监控/地图大屏/监控墙、AI 预测/诊断/生命周期/额度、**AI 原生增强（库存/成本分析、运维报告、故障/工单流程建议，AI 工作台）**、固件 OTA、**库存进销存（物料/供应商/采购/流水）、维修费用与工单领料成本闭环**、用户/角色/部门/审计/强密码、仪表盘与统计。**形态：Web 管理端（单端）。**

**首版发布不包含（延后）**：配置下发、远程重启、移动端、短信告警、多级审批、实景图层、MQTT 强认证（运维待开）。

---

*本报告基于代码静态审核 + 单元测试 + 生产 E2E 实测；性能并发指标未经压测，生产量级（百级设备）运行正常。*
